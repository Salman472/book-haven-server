# 📚 Book Haven – Server Side

Book Haven Server is the backend API for the Book Haven MERN application. It provides secure and efficient endpoints for managing books, users, and application data. Built with **Node.js**, **Express**, and **MongoDB**, this server handles all core logic and database operations for the platform.

---

## 🚀 Features

- RESTful API built using Express  
- MongoDB integration using the official MongoDB driver  
- Environment variable support with dotenv  
- CORS enabled for secure client–server communication  
- Clean, scalable code structure  

---

## 🛠️ Tech Stack

- **Node.js**
- **Express.js**
- **MongoDB**
- **dotenv**
- **CORS**

---

"dependencies": {
"cors": "^2.8.5",
"dotenv": "^17.2.3",
"express": "^5.1.0",
"mongodb": "^7.0.0"
}

1. **Clone the repository**
   ```bash
   git clone https://github.com/your-username/book-haven-server.git
   cd book-haven-server

Install dependencies

npm install

Start the server

npm start

